Place your Android release keystore in this directory.

Recommended file name:
- `upload-keystore.jks`

Then copy `android/key.properties.example` to `android/key.properties`
and replace the placeholder values with your real signing credentials.
